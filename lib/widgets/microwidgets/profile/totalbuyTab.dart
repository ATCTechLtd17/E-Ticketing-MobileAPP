import 'package:flutter/material.dart';

class TotalBuyTab extends StatelessWidget {
  const TotalBuyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}