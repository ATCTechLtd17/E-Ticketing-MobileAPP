import 'package:flutter/material.dart';

class CancelTicketTab extends StatelessWidget {
  const CancelTicketTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}